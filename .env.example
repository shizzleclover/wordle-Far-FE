# Optional: override socket origin (default: same host, port 3001)
# VITE_SERVER_URL=http://localhost:3001

# Optional: full API base (default: relative /api via Vite proxy)
# VITE_API_URL=http://localhost:3001
